let count = 0;

count++;
// count  += 1; can also be used to achieve the same result as count++ or count = count + 1
// Line 3 is updating the value of the count variable by adding 1 to its current value. The = operator is an assignment operator that assigns the result of the expression on the right (count + 1) back to the variable on the left (count). This effectively increments the count variable by 1 each time this line is executed.

// Line 1 is a variable declaration, creating the count variable with an initial value of 0
// Describe what line 3 is doing, in particular focus on what = is doing
